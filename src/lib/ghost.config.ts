import GhostConfig from "./config/GhostConfig";

const config: GhostConfig = {
    source: "onload",
    aliases: {
        colors: {
            primary: "FCCA46",
            primaryLight: "ffe7a1",
            secondary: "0C2233",
        }
    }
};

export default config;