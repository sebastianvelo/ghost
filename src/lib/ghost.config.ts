import GhostConfig from "./GhostConfig";

const config: GhostConfig = {
    aliases: {
        colors: {
            primary: "FCCA46",
            primaryLight: "ffe7a1",
            secondary: "0C2233",
        }
    }
};

export default config;